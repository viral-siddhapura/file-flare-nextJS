"use client";

import { useState } from "react";

export const LoginForm = () => {
    
    const [tab, setTab] = useState("account");
    
    return (
        <main className="flex flex-col items-center w-full max-w-lg mt-8">
            
        </main>
    )
}